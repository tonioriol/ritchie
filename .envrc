# Load devbox environment
eval "$(devbox generate direnv --print-envrc)"

# Load secrets from .env file
dotenv_if_exists

# Set KUBECONFIG to the neumann cluster kubeconfig
export KUBECONFIG="${PWD}/clusters/neumann/kubeconfig"
