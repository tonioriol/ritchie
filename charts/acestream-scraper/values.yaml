image:
  repository: pipepito/acestream-scraper
  tag: latest
  pullPolicy: Always

replicaCount: 1

service:
  port: 8000

env:
  # Timezone
  TZ: "Europe/Madrid"
  # Flask port
  FLASK_PORT: "8000"
  # Enable bundled acestream engine + Acexy (acestream-http-proxy)
  ENABLE_ACESTREAM_ENGINE: "true"
  ENABLE_ACEXY: "true"
  ENABLE_TOR: "false"
  ENABLE_WARP: "false"
  # Acexy runs on localhost:8080 inside the same container
  ACESTREAM_HTTP_HOST: "localhost"
  ACESTREAM_HTTP_PORT: "8080"
  # Allow remote access (needed for k8s service access)
  ALLOW_REMOTE_ACCESS: "yes"

persistence:
  enabled: true
  size: 1Gi
  storageClass: ""
  accessMode: ReadWriteOnce

ingress:
  # Traffic is served via Cloudflare Tunnel (no public Ingress required for now).
  enabled: false
  host: acestream-scraper.neumann.tonioriol.com
  tls:
    enabled: true
    issuer: letsencrypt-prod
    secretName: acestream-scraper-tls
