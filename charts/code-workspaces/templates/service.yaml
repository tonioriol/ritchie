apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    {{- include "code-workspaces.labels" . | nindent 4 }}
  annotations:
    external-dns.alpha.kubernetes.io/hostname: code.tonioriol.com
    external-dns.alpha.kubernetes.io/target: 85e6bc75-0025-4fc3-9341-d4e517fea614.cfargotunnel.com
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "code-workspaces.labels" . | nindent 4 }}
