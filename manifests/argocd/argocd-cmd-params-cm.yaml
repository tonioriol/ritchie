apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cmd-params-cm
  namespace: argocd
data:
  # TLS is terminated at Traefik; run ArgoCD server in insecure mode to avoid redirect loops.
  server.insecure: "true"
